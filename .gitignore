# Node
node_modules/
dist/
build/
*.log
npm-debug.log*

# Env / segredos
.env
.env.local
.env.*.local

# Evolution API (dados locais, não versionar)
evolution-api/.env
evolution-api/tmp/

# Supabase CLI (estado local, não versionar)
database/supabase/.temp/
database/supabase/.branches/

# Deploy de produção (segredos reais - os .example ficam versionados)
deploy/.env
deploy/backend.env
deploy/evolution-api.env

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
